import { StyleSheet } from 'react-native';
import { MARGIN_TOP_HEADER } from '../../constants/scale';

const styles = StyleSheet.create({
  container: {
    backgroundColor: 'white',
    paddingTop: 40,
  },
});

export default styles;
